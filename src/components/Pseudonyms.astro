---
import { pseudo } from "src/pages";
---

<details>
    <summary>I use two pseudonyms: Its-Just-Nans and {pseudo}</summary>
    <input type="radio" id="me" />
    <label for="me" id="label-me" data-tooltip="Click to see the truth !">
        <span id="colored">this.style.color</span> = 'Its-Just-Nans' == '{pseudo}' ? 'green' : 'red';
    </label>
</details>

<style>
    summary {
        cursor: pointer;
    }
    summary,
    label {
        font-family:
            system-ui,
            -apple-system,
            BlinkMacSystemFont,
            "Segoe UI",
            Roboto,
            Oxygen,
            Ubuntu,
            Cantarell,
            "Open Sans",
            "Helvetica Neue",
            sans-serif;
    }
    #me:checked ~ label #colored {
        color: green;
    }
    #label-me {
        cursor: pointer;
        font-weight: bold;
    }
    #me {
        display: none;
    }
</style>
