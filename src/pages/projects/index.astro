---
import BaseLayout from "@layouts/BaseLayout.astro";
import ProjectsCompo from "@components/Projects.astro";
import { pseudo } from "@scripts/pages";
---

<BaseLayout
    title="Projects"
    addTitle
    description=`Discover ${pseudo}'s projects`
    keywords={[`${pseudo} projects`, "projects", "pull requests"]}
>
    <ProjectsCompo />
</BaseLayout>
