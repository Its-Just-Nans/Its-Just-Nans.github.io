---
import type { InferGetStaticPropsType } from "astro";
import SimpleArticleSlug from "@components/SimpleArticleSlug.astro";
import { getStaticPathsByTag } from "@components/utils";

export const getStaticPaths = getStaticPathsByTag("school");

type Props = InferGetStaticPropsType<typeof getStaticPaths>;

const { post } = Astro.props;
const { Content, frontmatter } = post;
const { date, title } = frontmatter;
---

<SimpleArticleSlug title={title} date={date}>
    <Content />
</SimpleArticleSlug>
